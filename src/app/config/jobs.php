<?php

	return [

		// url base for running jobs
		'base_url' => 'jobs',

		// secure token that should prevent attack through running jobs
		// overwrite in config/local folder with something unique to your app!!
		'security_token' => 'jobs123',

	];
